#!/usr/bin/env ruby

require 'bundler/setup'
require 'octokiq'

require_relative '../examples/plain_ruby/dummy_worker'

Octokiq.configure do |c|
  c.queues = %w[default high medium low]
end

cli = Octokiq::CLI.new
cli.run
